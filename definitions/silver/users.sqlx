config {
  type: "table", 
  database: "chavysb",
  dataset: "silver", 
  description: "Clean users table - Full Load",
  assertions: {
    uniqueKey: ["user_id"],    
    nonNull: ["user_id","name", "email"]
  }
}

SELECT 
  user_id,
  name,
  email
FROM ${ref("bronze", "users")} 
